<Page x:Class="CapstoneEmployeeScheduler.Views.History"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CapstoneEmployeeScheduler.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="History">

    <Grid>
        <Grid Margin="0,0,-700,-450">

            <Label x:Name="Title" Content="GeneratedSchedules" HorizontalContentAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="251,70,0,0" Width="653" Height="78" FontSize="36" BorderThickness="0" SnapsToDevicePixels="True" FontWeight="Bold"/>
            <ListView  Name= "GeneratedSchedules" SelectionChanged="History_SelectionChanged" Margin="193,205,104,211">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Date and Time Created" Width="400" />
                        <GridViewColumn>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <Button Content="View"
                                                Click="view_Click"
                                 Command="{Binding
                                 Path=DataContext.RunCommand,
                                 RelativeSource={RelativeSource FindAncestor, 
                                 AncestorType= {x:Type ItemsControl}}}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn/>
                    </GridView>
                </ListView.View>
            </ListView>
            <Button x:Name="CSVEButton" Content="Export Employees to CSV" HorizontalAlignment="Left" Height="58" Margin="610,588,0,0" VerticalAlignment="Top" Width="286" FontWeight="Bold" Click="CSVEButton_Click" FontSize="20"/>
            <Button x:Name="CSVRButton" Content="Export Roles to CSV" HorizontalAlignment="Left" Height="58" Margin="193,588,0,0" VerticalAlignment="Top" Width="286" FontWeight="Bold" Click="CSVRButton_Click" FontSize="20"/>
        </Grid>
        <Grid/>
    </Grid>
</Page>
